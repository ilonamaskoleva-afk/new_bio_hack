# Настройка PubMed API

## ✅ API Ключ установлен

**API Key:** `72feaa1f188b673920ae39c39985d2535608`

API ключ сохранен в файле `.env` и автоматически загружается при запуске приложения.

## 📋 Что изменилось

### 1. Использование API ключа

- ✅ API ключ автоматически загружается из `.env` или конфига
- ✅ Устанавливается в `Entrez.api_key` при инициализации PubMedScraper
- ✅ Увеличивает лимит запросов с 3 до 10 запросов в секунду

### 2. Улучшенный парсинг CVintra

Добавлены дополнительные regex паттерны для более точного извлечения:

- `cv intra-subject: X%`
- `intra-subject cv: X%`
- `cv intra: X%`
- `intra-individual cv: X%`
- `within-subject cv: X%`
- `cv intra-subject = X%`
- И другие варианты

### 3. Улучшенный парсинг PK параметров

**Cmax:**
- Поддержка различных единиц (ng/ml, mg/l, μg/ml, mcg/ml)
- Альтернативные названия (maximum concentration, peak concentration)

**AUC:**
- Поддержка различных форматов (AUC0-t, AUC0-∞)
- Различные единицы измерения

**Tmax:**
- Альтернативные названия (time to Cmax, time to maximum concentration)

**T½:**
- Различные варианты написания (half-life, elimination half-life, terminal half-life)

### 4. Оптимизация скорости

- С API ключом: задержка между запросами 0.1 сек (10 req/sec)
- Без API ключа: задержка 0.5 сек (3 req/sec)

### 5. Логирование

- Логируется установка API ключа
- Логируется количество найденных значений CVintra
- Улучшенное логирование процесса поиска

## 🧪 Тестирование

Запустите тест для проверки работы API и парсинга:

```bash
py test_pubmed_api.py
```

Тест проверит:
- ✅ Установку API ключа
- ✅ Поиск статей для разных препаратов
- ✅ Извлечение CVintra из статей
- ✅ Извлечение PK параметров
- ✅ Корректность рекомендаций дизайна

## 📊 Примеры результатов

### Aspirin
- CVintra: ~15%
- Дизайн: 2×2 Cross-over

### Metformin
- CVintra: ~35%
- Дизайн: 3-way Replicate

### Propranolol
- CVintra: ~55%
- Дизайн: 4-way Replicate (RSABE)

## ⚙️ Конфигурация

API ключ настраивается через:

1. **Файл `.env`** (приоритет 1):
   ```
   NCBI_API_KEY=72feaa1f188b673920ae39c39985d2535608
   NCBI_EMAIL=research@example.com
   ```

2. **Переменные окружения** (приоритет 2):
   ```bash
   set NCBI_API_KEY=72feaa1f188b673920ae39c39985d2535608
   ```

3. **Конфиг** (приоритет 3):
   ```python
   Config.NCBI_API_KEY = "72feaa1f188b673920ae39c39985d2535608"
   ```

## 🔍 Проверка работы

При запуске приложения вы должны увидеть в логах:

```
✅ PubMed API ключ установлен: 72feaa1f18...
```

Если ключ не установлен:

```
⚠️ PubMed API ключ не установлен. Лимит запросов: 3 запроса/сек
```

## 📝 Примечания

- API ключ увеличивает лимит запросов, но не обязателен для работы
- Без ключа система работает с лимитом 3 запроса/сек
- Парсинг работает на основе regex, для более точного извлечения рекомендуется использовать LLM
